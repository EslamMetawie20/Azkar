{
  "name": "@azkar/backend",
  "version": "1.0.0",
  "description": "Spring Boot backend for Azkar app",
  "scripts": {
    "dev": "mvn spring-boot:run",
    "build": "mvn -v >nul 2>&1 && mvn clean package || echo 'Maven not found, skipping backend build'",
    "test": "mvn test",
    "lint": "echo 'No linting configured for Java backend'",
    "format": "echo 'No formatting configured for Java backend'"
  },
  "keywords": ["azkar", "spring-boot", "java"],
  "author": "",
  "license": "MIT"
}